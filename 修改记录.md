# BB-Memory 修改记录

本文档记录面向 SillyTavern 的版本变更摘要（中文）。完整历史见 `README.md` 中的「版本记录」一节。

---

## v2.6.1（2026-05-03）

### 目的

对齐 [SillyTavern 官方扩展文档](https://docs.sillytavern.app/for-contributors/writing-extensions)，修复在酒馆内「设置面板不显示、命令或交互异常」等与挂载路径、DOM 时机、API 差异相关的问题；**不改变记忆检索、注入、存储等业务逻辑**。

### 变更摘要

1. **扩展目录解析**：从 `extensions.js` 引入官方 `findExtension('BB-Memory')`，作为 `renderExtensionTemplateAsync(extensionFolder, …)` 的首选路径来源；仅在失败时回退到解析 `import.meta.url`。纠正原先错误的默认回退路径（易与真实文件夹大小写不一致）。
2. **设置面板挂载**：在 `#extensions_settings2` 与 `#extensions_settings` 之间择优插入，并在短时间内重试，避免扩展面板 DOM 尚未创建导致静默失败。
3. **斜杠命令**：优先使用 `SlashCommandParser.addCommandObject(SlashCommand.fromProps(…))`，异常时回退 `registerSlashCommand`；统一处理新版解析器可能传入的字符串或无名参数数组。
4. **确认弹窗**：删除/清空等确认逻辑改为与 `POPUP_RESULT.AFFIRMATIVE`（不存在时回退数值 `1`）比较。
5. **事件 API**：监听聊天相关事件时同时兼容 `event_types` 与 `eventTypes`。
6. **文档**：更新中文版 `README.md`（安装路径、兼容性说明、API 表）；新增本文件。

### 修改文件列表

- `index.js`
- `memory-assistant.js`
- `manifest.json`（版本号 2.6.1）
- `README.md`
- `修改记录.md`（本文件）
